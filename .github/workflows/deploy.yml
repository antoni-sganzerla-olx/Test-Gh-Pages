name: Criar pasta e arquivo HTML no Github Pages

on:
  workflow_dispatch:
    inputs:
      platform_name:
        description: 'Nome da plataforma'
        required: true
        type: string
env:
  PLATFORM_EMOJI:

permissions:
    id-token: write
    pages: write
    contents: write

jobs:
  generate-content:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Definir nome da plataforma
        id: set_platform_name
        run: |
          echo "Nome da plataforma: ${{ inputs.platform_name }}"
          echo "::set-output name=platform_name::${{ inputs.platform_name }}"

      - name: Criar pasta
        run: |
          mkdir -p "public"

      - name: Criar arquivo index.html
        run: |
          echo "<html><body><h1>${{ inputs.platform_name }}</h1></body></html>" > "public/index.html"

      - name: Define platform emoji
        run: |
          if [ "${{ inputs.platform_name }}" == "ios" ]; then
            echo "PLATFORM_EMOJI=:apple:"
          elif [ "${{ inputs.platform_name }}" == "android" ]; then
            echo "PLATFORM_EMOJI=:robot:"
          fi

      - name: print
        run: |
          echo $PLATFORM_EMOJI

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: public
          branch: reports
          target-folder: ${{ inputs.platform_name }}

